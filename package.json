{
  "name": "wwc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A 2D side-scrolling post-apocalyptic survival game. Brutal outside, cozy inside.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "playwright-core": "^1.61.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.8",
    "vitest": "^2.1.2"
  }
}
